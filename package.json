{
    "name": "youcert-app",
    "private": true,
    "description": "Cloudflare Worker entry point for YOUCERT Flask container",
    "scripts": {
        "deploy": "wrangler deploy",
        "dev": "wrangler dev"
    },
    "dependencies": {
        "@cloudflare/containers": "^0.1.1",
        "dotenv": "^17.3.1"
    },
    "devDependencies": {
        "wrangler": "^4.0.0"
    }
}
